# 前端环境变量示例（可复制为 .env 或 .env.local）

# 后端 API 基地址（必须与 NestJS 服务实际地址一致）
# 本地开发通常为：
VITE_API_BASE_URL=http://localhost:3000

# 比赛 / 学校信息（可选，未设置则使用 constants.ts 中的默认值）
# VITE_CONTEST_NAME=2025图形化编程创意赛
# VITE_SCHOOL_NAME=中山市光正实验学校

# 报名截止时间（可选），ISO 8601 字符串。
# 不设置则默认“当前时间 + 3 天”。
# 示例：
# VITE_CONTEST_DEADLINE=2025-06-30T23:59:59.000Z

